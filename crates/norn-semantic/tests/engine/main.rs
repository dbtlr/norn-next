//! The engine suite: sidecar lifecycle, the model gate, drain semantics, the
//! convergence bar, and nearest.

mod common;

mod convergence;
mod drain;
mod errors;
mod firewall;
mod lifecycle;
mod model;
mod nearest;
