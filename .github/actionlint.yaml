self-hosted-runner:
  # The scheduled macOS certification lane: a repository-scoped runner inside
  # a macOS 15 virtual machine on Apple silicon (.github/workflows/certify.yml).
  labels:
    - macos-norn-soak
